<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:backgroundTint="@color/black">

    <ImageView
        android:id="@+id/grid_image"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:src="@drawable/tumbleweed" />

    <TextView
        android:id="@+id/grid_image_description"
        android:visibility="invisible"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:textColor="@color/black"
        android:text="example"
        android:textStyle="bold"
        android:background="@drawable/image_description_text"
        android:layout_alignBottom="@+id/grid_image"
        android:layout_alignStart="@+id/grid_image"
        android:layout_alignEnd="@+id/grid_image"
        android:textAlignment="center" />

</RelativeLayout>